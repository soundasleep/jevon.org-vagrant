source 'https://rubygems.org'

# These are only the gems necessary to run `cucumber` tests
# on the deployed Vagrant VM. They aren't used on the deployed
# machine itself.

gem 'activesupport', '~> 4.2'

gem 'rspec', require: false
gem 'rspec-collection_matchers'
gem 'cucumber'

# Testing support
gem 'net-ssh'
